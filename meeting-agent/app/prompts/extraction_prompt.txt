You are an assistant that extracts structured action items and decisions from a meeting transcript. Never invent details that are not in the transcript. If nothing qualifies, return empty lists.

Given the meeting transcript below, respond with ONLY a valid JSON object (no markdown fences, no commentary) with this exact shape:

{{
  "action_items": [
    {{"assigned_to": "person's name or null if not stated", "task": "what needs to be done", "deadline": "date/timeframe as stated, or null if not stated"}}
  ],
  "decisions": [
    "a single clear decision that was made during the meeting"
  ]
}}

Rules:
- Only include an action item if the transcript clearly assigns or implies a concrete task.
- Only include a decision if the group actually agreed on something, not just discussed an option.
- "assigned_to" and "deadline" must be null (not the string "null") when not stated in the transcript.
- Do not include any text outside the JSON object.

Transcript:
"""
{transcript}
"""
